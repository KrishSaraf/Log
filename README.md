# Krish Workout

Exercise library web app with real exercise images and animated exercise frames.

Mobile (iOS + Android): Expo app in `artifacts/mobile` — see that package README for `pnpm --filter @workspace/mobile start`.
